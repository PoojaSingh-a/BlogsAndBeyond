<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Blog-Blog & Beyond</title>
    <script src="https://cdn.tailwindcss.com"></script>    
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
</head>
<body class="overflow-hidden">
    <div class="header p-5 text-white text-2xl flex items-center justify-between bg-black">
        <a href="/" class="flex items-center">
            <i class="fa-solid fa-house text-white text-3xl mr-3"></i>
        </a>
        <h1>Do the desired changes</h1>
        <h1></h1>
    </div>
        <div class="bg-blue-300 min-h-screen flex justify-center">
            <form action="/editBlog/<%= Blog._id%>" method="post" class="bg-white h-full mt-20 p-10 rounded-lg shadow-lg w-1/2">
                <div class="mb-4 mt-5">
                    <label for="btitle" class="block text-gray-700 font-medium mb-1">Blog Content</label>
                    <input type="text" id="btitle" name="btitle" class="w-full border border-gray-300 rounded-md p-2 " value="<%= Blog.btitle %>">
                </div>
                <div class="mb-4">
                    <label for="bcontent" class="block text-gray-700 font-medium mb-1">Blog Content</label>
                    <textarea id="bcontent" name="bcontent" class="w-full border border-gray-300 rounded-md p-2" rows="8"><%= Blog.bcontent %> </textarea>
                </div>
                <button type="submit" class="w-full bg-blue-700 text-white font-bold py-2 px-4 rounded hover:bg-yellow-600">Update Blog</button>
                <button type="button" class="mt-2 w-full bg-zinc-500 text-white font-bold py-2 px-4 rounded hover:bg-zinc-800" onclick="goback()">Go Back</button>
            </form>
            
</div>
</body>
<script>
    function goback(){
        history.back()
    }
</script>
</html>